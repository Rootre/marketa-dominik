    {{toUpperCase name}}_CREATE_URL: `${API_URL}{{toLowerCase name}}/create`,
    {{toUpperCase name}}_DELETE_URL: `${API_URL}{{toLowerCase name}}/delete`,
    {{toUpperCase name}}_READ_URL: `${API_URL}{{toLowerCase name}}/read`,
    {{toUpperCase name}}_UPDATE_URL: `${API_URL}{{toLowerCase name}}/update`,
};